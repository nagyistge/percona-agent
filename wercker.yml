box: wercker/golang
services:
    - wercker/mysql
env:
  PCT_TEST_MYSQL_DSN: $$WERCKER_MYSQL_URL$$
build:
  steps:
    - setup-go-workspace
    - script:
        name: run tests
        code: |
          export GOPATH="$GOPATH:$WERCKER_SOURCE_DIR"
          test/runner.sh -u
